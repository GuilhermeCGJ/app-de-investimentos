import React from "react";
import { render, screen, fireEvent } from "@testing-library/react";
import { BrowserRouter } from 'react-router-dom'
import Login from "../pages/Login";
import Home from "../pages/Home";

describe('Testando a página principal', () => {
  test('Testa se o header aparece na página principal', () => {

  });

  test('Testa se aparecem as áreas de ações do usuário e ações disponíveis', () => {
    
  });

  test('Testa se renderiza todas as ações disponíveis', () => {
    
  });

  test('Testa se o botão para saque e depósito aparece na página principal', () => {
    
  });

  test('Testa se o mercado de ações abre ao clicar para negociar uma ação', () => {
    
  });
})